// Enables one of the core's Stimulus controllers under the identifier the
// core's own markup asks for. assets/controllers.json cannot do it — see
// "Enabling the core's Stimulus controllers" in docs/architecture.md.
export { default } from '@uhifadhi/shell-bundle/controllers/sidebar_tree_controller.js';
